import StatsModel from './stasts-model';

export default class ParticipantModel {
    participantID: number;
    summonerName: string;
    championID: number;
    teamID: number;
    stats: StatsModel;
}
